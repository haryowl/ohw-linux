const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");// Replace the entire problematic section with just the order clausecontent = content.replace(/include: \[\{[\s\S]*?model: require\('\.\.\/models'\)\.Device[\s\S]*?\}\]/g, "");// Also try a more specific approach - replace the exact problematic linescontent = content.replace("include: [{\n                model: require('../models').Device,\n                as: 'device',\n                attributes: ['name'],\n                required: false\n            }]", "");// Remove any remaining Device include referencescontent = content.replace(/include: \[\{[\s\S]*?model: require\('\.\.\/models'\)\.Device[\s\S]*?\}\]/g, "");fs.writeFileSync("src/routes/records.js", content);console.log("? REMOVED DEVICE INCLUDE - Export should work now"); 
