const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");// Remove the problematic Device include by replacing the entire problematic sectioncontent = content.replace(/include: \[\{[\s\S]*?model: require\('\.\.\/models'\)\.Device[\s\S]*?\}\]/g, "");// Also try a more specific approach - remove the exact problematic linescontent = content.replace("include: [{\n                model: require('../models').Device,\n                as: 'device',\n                attributes: ['name'],\n                required: false\n            }]", "");// Remove any remaining Device include referencescontent = content.replace(/include: \[\{[\s\S]*?model: require\('\.\.\/models'\)\.Device[\s\S]*?\}\]/g, "");fs.writeFileSync("src/routes/records.js", content);console.log("? PERMANENT FIX APPLIED: Removed Device include"); 
