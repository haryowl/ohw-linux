const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");content = content.replace("include: [{\n                model: require('../models').Device,\n                as: 'device',\n                attributes: ['name'],\n                required: false\n            }]", "");fs.writeFileSync("src/routes/records.js", content);console.log("FIXED: Removed Device include"); 
