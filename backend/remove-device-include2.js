const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");// Remove the problematic include by targeting the exact formattingcontent = content.replace(/include: \[\{[\s\S]*?model: require\('\.\.\/models'\)\.Device[\s\S]*?\}\]/g, "");// Also try removing the specific lines one by onecontent = content.replace("include: [{", "");content = content.replace("model: require('../models').Device,", "");content = content.replace("as: 'device',", "");content = content.replace("attributes: ['name'],", "");content = content.replace("required: false", "");content = content.replace("}]", "");fs.writeFileSync("src/routes/records.js", content);console.log("? REMOVED DEVICE INCLUDE - Export should work now"); 
