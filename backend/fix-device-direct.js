const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");// Remove the problematic Device include by targeting the exact linescontent = content.replace("include: [{\n                model: require('../models').Device,\n                as: 'device',\n                attributes: ['name'],\n                required: false\n            }]", "");fs.writeFileSync("src/routes/records.js", content);console.log("? REMOVED DEVICE INCLUDE - This should work now"); 
