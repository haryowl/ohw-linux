const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");content = content.replace("const { requireAuth } = require('../middleware/auth');", "const { requireAuth } = require('./auth');");content = content.replace("const { filterDevicesByPermission } = require('../middleware/permissions');", "const { filterDevicesByPermission } = require('../middleware/permissions');");fs.writeFileSync("src/routes/records.js", content);console.log("Fixed import paths in records.js"); 
