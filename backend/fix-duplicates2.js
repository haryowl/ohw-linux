const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");// Remove ALL duplicate requireAuth and filterDevicesByPermission importscontent = content.replace(/const \{ requireAuth \} = require\('\.\/auth'\);\n/g, "");content = content.replace(/const \{ filterDevicesByPermission \} = require\('\.\.\/middleware\/permissions'\);\n/g, "");// Add them back once after ExcelJScontent = content.replace("const ExcelJS = require('exceljs');", "const ExcelJS = require('exceljs');\nconst { requireAuth } = require('./auth');\nconst { filterDevicesByPermission } = require('../middleware/permissions');");fs.writeFileSync("src/routes/records.js", content);console.log("Fixed duplicate imports"); 
