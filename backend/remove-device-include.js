const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");// Find and replace the exact problematic sectionconst problematicSection = `include: [{                model: require('../models').Device,                as: 'device',                attributes: ['name'],                required: false            }]`;content = content.replace(problematicSection, "");fs.writeFileSync("src/routes/records.js", content);console.log("? REMOVED DEVICE INCLUDE - Export should work now"); 
