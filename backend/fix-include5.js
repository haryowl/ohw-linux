const fs = require("fs");let content = fs.readFileSync("src/routes/records.js", "utf8");// Find and replace the problematic include sectionconst problematicInclude = `include: [{                model: require('../models').Device,                as: 'device',                attributes: ['name'],                required: false            }]`;content = content.replace(problematicInclude, "");fs.writeFileSync("src/routes/records.js", content);console.log("Removed problematic include"); 
